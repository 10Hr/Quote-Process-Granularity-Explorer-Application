<UserControl x:Class="ZSExplorer.RightPanel"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:ZSExplorer"
             MinWidth="350" MinHeight="600"
             Background="White">

    <Border BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="2" CornerRadius="8" Padding="10" Margin="5">
        <StackPanel Orientation="Vertical" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">

            <!-- Header -->
            <Border Background="{StaticResource PrimaryBrush}" CornerRadius="6" Padding="8" Margin="0 0 0 10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Left side: Symbol and option details -->
                    <StackPanel Orientation="Vertical" Grid.Column="0">
                        <TextBlock x:Name="ContractSymbolText" 
                                   Text="CONTRACT SYMBOL" 
                                   FontWeight="Bold" 
                                   FontSize="16" 
                                   Foreground="White" />
                        <TextBlock x:Name="OptionDetailsText" 
                                   Text="Underlying | Expiration | Strike" 
                                   FontSize="12" 
                                   Foreground="White" 
                                   Margin="0,4,0,0" />
                    </StackPanel>

                    <!-- Right side: Status and Remove button -->
                    <StackPanel Orientation="Horizontal" Grid.Column="1" VerticalAlignment="Center">
                        <!-- Status indicator -->
                        <Ellipse x:Name="StatusIndicator" Width="14" Height="14" Fill="Red" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <!-- Remove button -->
                        <Button x:Name="RemoveButton" Content="Remove" Width="Auto" Height="24" FontWeight="Bold" 
                                Foreground="White" Background="{StaticResource PrimaryPressedBrush}" 
                                BorderThickness="0" Cursor="Hand" Click="RemoveKsTest"/>
                    </StackPanel>

                </Grid>
            </Border>

            <!-- Control Body: Two-column grid -->
            <Grid Margin="0 0 0 10" VerticalAlignment="Top" HorizontalAlignment="Stretch" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <!-- Left column: Time window config & sample statistics -->
                <StackPanel Grid.Column="0" Margin="0,0,10,0" VerticalAlignment="Top">

                    <!-- Time Window Configuration-->
                    <!-- <TextBlock Text="Time Window Configuration" FontWeight="Bold" Margin="0,0,0,6" />
                    <Slider x:Name="TimeWindowSlider" Minimum="1" Maximum="100" Value="1" TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="TimeWindowSlider_ValueChanged" />
                    <TextBlock x:Name="TimeWindowValueText" Text="1 (units)" HorizontalAlignment="Right" Margin="0,4,0,10" /> -->


<!-- Time Window Configuration -->
<StackPanel Orientation="Vertical" Margin="0,0,0,10">
    <TextBlock Text="📉 Log Return Time Window" FontWeight="Bold" FontSize="16" />
    <TextBlock Text="Return Calculation Window (Microseconds)" FontWeight="Bold" />

    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,5,0,0">
        <Slider x:Name="TimeWindowSlider"
                Width="300"
                Minimum="0"
                Maximum="100"
                TickFrequency="1"
                IsSnapToTickEnabled="True"
                ValueChanged="TimeWindowSlider_ValueChanged"
                PreviewMouseDown="TimeWindowSlider_PreviewMouseDown"/>
        <TextBox x:Name="MicrosecondInputBox"
                 Width="80"
                 Margin="10,0,0,0"
                 VerticalContentAlignment="Center"
                 HorizontalContentAlignment="Center"
                 TextChanged="MicrosecondInputBox_TextChanged"
                 Text="0"/>
    </StackPanel>
    <StackPanel Orientation="Horizontal" Margin="10">
        <CheckBox x:Name="BidOnlyCheckbox" Content="Use Bid Ask" Margin="5" />
    </StackPanel>

    <TextBlock x:Name="TimeWindowValueText"
               FontWeight="SemiBold"
               Margin="0,5,0,0"
               FontSize="12"/>
</StackPanel>










                    <!-- <StackPanel Margin="20" Orientation="Vertical">
                        <TextBlock Text="📉 Log Return Time Window" FontWeight="Bold" FontSize="16" />
                        
                        <TextBlock Text="Return Calculation Window" Margin="0,10,0,2" />
                        
                        <Grid>
                            <Slider x:Name="TimeWindowSlider"
                                    Minimum="0"
                                    Maximum="10"
                                    TickFrequency="1"
                                    IsSnapToTickEnabled="True"
                                    ValueChanged="TimeWindowSlider_ValueChanged"
                                    VerticalAlignment="Center" />
                            
                            <TextBlock x:Name="TimeWindowValueText"
                                    Text="0 minutes"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Margin="0,0,10,0"
                                    FontWeight="Bold"
                                    Foreground="Blue" />
                        </Grid>
                        
                        <Canvas x:Name="TimeSliderLabelsCanvas" Height="25" Margin="0,5" />
                        
                        <Border Background="#f8f8f8" Padding="10" CornerRadius="5" Margin="0,10,0,0" BorderBrush="#ddd" BorderThickness="1">
                            <TextBlock Text="🧠 Calculation Method: Log returns computed from back pivot with forward-fill for missing data points"
                                    TextWrapping="Wrap"
                                    FontSize="12"
                                    FontStyle="Italic" />
                        </Border>
                    </StackPanel> -->

                    <!-- Sample Statistics Grid -->
                    <TextBlock Text="Sample Statistics" FontWeight="Bold" Margin="0,0,0,6" />
                    <UniformGrid Columns="2" Rows="3" Margin="0,0,0,10">
                        <TextBlock Text="Sample Size:" />
                        <TextBlock x:Name="SampleSizeText" Text="-" />

                        <TextBlock Text="Mean Return:" />
                        <TextBlock x:Name="MeanReturnText" Text="-" />

                        <TextBlock Text="Standard Deviation:" />
                        <TextBlock x:Name="StdDevText" Text="-" />
                    </UniformGrid>
                </StackPanel>

                <!-- Right column: Fitted distribution & KS test -->
                <StackPanel Grid.Column="1" VerticalAlignment="Top">

                    <!-- Fitted Distribution Parameters -->
                    <TextBlock Text="Fitted Distribution Parameters" FontWeight="Bold" Margin="0,0,0,6" />
                    <UniformGrid Columns="2" Rows="4" Margin="0,0,0,10">
                        <TextBlock Text="Location Param:" />
                        <TextBlock x:Name="LocationParamText" Text="-" />

                        <TextBlock Text="Scale Param:" />
                        <TextBlock x:Name="ScaleParamText" Text="-" />

                        <TextBlock Text="Degrees of Freedom:" />
                        <TextBlock x:Name="DegreesFreedomText" Text="-" />

                        <TextBlock Text="Convergence Iterations:" />
                        <TextBlock x:Name="ConvergenceIterationsText" Text="-" />
                    </UniformGrid>

                    <!-- KS Test Results -->
                    <Border BorderBrush="{StaticResource SecondaryBrush}" BorderThickness="1" CornerRadius="6" Padding="8" Background="#FFF9F2D6" >
                        <StackPanel>
                            <TextBlock Text="KS Test Results" FontWeight="Bold" Margin="0,0,0,6" />

                            <TextBlock x:Name="KsTestStatText" Text="Test Statistic: -" FontSize="18" FontWeight="Bold" />
                            <TextBlock x:Name="StatDecisionText" Text="Decision: -" FontWeight="Bold" Margin="0,2,0,6" />
                            <TextBlock x:Name="PValueText" Text="P-value: -" FontSize="12" />
                            <TextBlock x:Name="CriticalValueText" Text="Critical value: -" FontSize="12" />
                        </StackPanel>
                    </Border>

                </StackPanel>
            </Grid>

            <!-- ECDF Plot Area -->
            <Border BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="1" CornerRadius="6" Background="White" Height="180" Margin="0,0,0,10">
                <Canvas x:Name="EcdfPlotCanvas" />
            </Border>

            <!-- Convergence Info -->
            <UniformGrid Columns="4" Rows="1" HorizontalAlignment="Stretch" >
                <StackPanel>
                    <TextBlock Text="Iterations" FontWeight="Bold" />
                    <TextBlock x:Name="ConvIterationsText" Text="-" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Error Tolerance" FontWeight="Bold" />
                    <TextBlock x:Name="ErrorToleranceText" Text="-" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Fit Quality" FontWeight="Bold" />
                    <TextBlock x:Name="FitQualityText" Text="-" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Status" FontWeight="Bold" />
                    <TextBlock x:Name="FitStatusText" Text="-" />
                </StackPanel>
            </UniformGrid>

                    <Border Grid.Row="4" BorderBrush="LightGray" BorderThickness="1" CornerRadius="4" Padding="10">
                        <StackPanel VerticalAlignment="Top">
                        <Grid Grid.Column="1" Margin="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <DataGrid x:Name="MarkDataGrid" 
                                AutoGenerateColumns="False" 
                                AutoGeneratingColumn="MarketDataGrid_AutoGeneratingColumn"
                                Grid.Row="1"
                                CanUserAddRows="False">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Symbol" Binding="{Binding Symbol}" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="datetime" Binding="{Binding DateTime, StringFormat=MM/dd/yyyy HH:mm:ss.ffffff}" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="MMID" Binding="{Binding MMID}" IsReadOnly="True"/>
                                    <DataGridCheckBoxColumn Header="BidAsk" Binding="{Binding BidAsk}" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="Price" Binding="{Binding Price}" IsReadOnly="True"/>
                                </DataGrid.Columns>
                            </DataGrid>

                        </Grid>
                        </StackPanel>
                    </Border>




        </StackPanel>
    </Border>
</UserControl>
