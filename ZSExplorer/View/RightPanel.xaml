<UserControl x:Class="ZSExplorer.RightPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:oxy="http://oxyplot.org/wpf"
             mc:Ignorable="d"
             MinWidth="350"
             Background="White">

    <Border BorderBrush="{StaticResource PrimaryBrush}"
            BorderThickness="2"
            CornerRadius="8"
            Padding="10"
            Margin="5">

        <!-- ENTIRE PANEL SCROLLS -->
        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">

            <StackPanel>

                <!-- ================= HEADER ================= -->
                <Border Background="{StaticResource PrimaryBrush}"
                        CornerRadius="6"
                        Padding="8"
                        Margin="0,0,0,10">

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock Text="{Binding ContractSymbolText}"
                                       FontWeight="Bold"
                                       FontSize="16"
                                       Foreground="White"/>

                            <TextBlock Text="{Binding OptionDetailsText}"
                                       FontSize="12"
                                       Foreground="White"
                                       Margin="0,4,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1"
                                    Orientation="Horizontal"
                                    VerticalAlignment="Center">

                            <Ellipse Width="14"
                                     Height="14"
                                     Fill="{Binding StatusIndicatorBrush}"
                                     Margin="0,0,10,0"/>

                            <Button Content="Remove"
                                    Padding="10,2"
                                    FontWeight="Bold"
                                    Foreground="White"
                                    Background="{StaticResource PrimaryPressedBrush}"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    Command="{Binding RemoveKsTestCommand}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- ================= CONTROLS ================= -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MaxWidth="350"/>
                        <ColumnDefinition Width="*" MaxWidth="350"/>
                    </Grid.ColumnDefinitions>

                    <!-- LEFT -->
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">

                        <TextBlock Text="Log Return Time Window"
                                   FontWeight="Bold"
                                   FontSize="15"/>

                        <TextBlock Text="Return Calculation Window (Seconds)"
                                   FontWeight="SemiBold"
                                   Margin="0,4,0,6"/>

                        <StackPanel Orientation="Horizontal">
                            <Slider Width="220"
                                    Minimum="{Binding TimeWindowSliderMinimum}"
                                    Maximum="{Binding TimeWindowSliderMaximum}"
                                    Value="{Binding TimeWindowSliderValue,
                                            Mode=TwoWay,
                                            UpdateSourceTrigger=PropertyChanged}"
                                    TickFrequency="1"
                                    IsMoveToPointEnabled="True"/>

                            <TextBox Width="70"
                                     Margin="8,0,0,0"
                                     TextAlignment="Center"
                                     VerticalContentAlignment="Center"
                                     Text="{Binding MicrosecondInputText,
                                            Mode=TwoWay,
                                            UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                            <TextBlock Width="50" Text="{Binding TimeLabel0Text}"/>
                            <TextBlock Width="50" TextAlignment="Center" Text="{Binding TimeLabel25Text}"/>
                            <TextBlock Width="50" TextAlignment="Center" Text="{Binding TimeLabel50Text}"/>
                            <TextBlock Width="50" TextAlignment="Center" Text="{Binding TimeLabel75Text}"/>
                            <TextBlock Width="50" TextAlignment="Right" Text="{Binding TimeLabel100Text}"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                            <CheckBox Content="Use Bid Ask"
                                      Margin="0,0,10,0"
                                      IsChecked="{Binding UseBidPrices}"/>

                            <CheckBox Content="Analyze All Call or Put Options"
                                      IsChecked="{Binding AnalyzeAllOptions}"/>
                        </StackPanel>

                        <TextBlock Text="Sample Statistics"
                                   FontWeight="Bold"
                                   Margin="0,10,0,6"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Sample Size:"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SampleSizeText}" 
                                       TextAlignment="Left" TextTrimming="CharacterEllipsis"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Mean Return:"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding MeanReturnText}"
                                       TextAlignment="Left" TextTrimming="CharacterEllipsis"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Std Dev:"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding StdDevText}"
                                       TextAlignment="Left" TextTrimming="CharacterEllipsis"/>
                        </Grid>
                    </StackPanel>

                    <!-- RIGHT -->
                    <StackPanel Grid.Column="1">

                        <TextBlock Text="Fitted Distribution Parameters"
                                   FontWeight="Bold"
                                   Margin="0,0,0,6"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="65"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Location:"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding LocationParamText}"
                                       TextAlignment="Left" TextTrimming="CharacterEllipsis"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Scale:"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding ScaleParamText}"
                                       TextAlignment="Left" TextTrimming="CharacterEllipsis"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="DoF:"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding DegreesFreedomText}"
                                       TextAlignment="Left" TextTrimming="CharacterEllipsis"/>
                        </Grid>

                        <Border Margin="0,10,0,0"
                                Padding="8"
                                Background="#FFF9F2D6"
                                BorderBrush="{StaticResource SecondaryBrush}"
                                BorderThickness="1"
                                CornerRadius="6">

                            <StackPanel HorizontalAlignment="Stretch">
                                <TextBlock Foreground="Red"
                                           FontWeight="Bold"
                                           Text="{Binding InsufficientDataText}"
                                           TextWrapping="Wrap"/>

                                <TextBlock Text="KS Test Results"
                                           FontWeight="Bold"
                                           Margin="0,6,0,4"/>

                                <TextBlock Text="{Binding KsTestStatText}"
                                           FontSize="18"
                                           FontWeight="Bold"
                                           TextTrimming="CharacterEllipsis"/>

                                <TextBlock Text="{Binding StatDecisionText}"
                                           FontWeight="Bold"
                                           TextTrimming="CharacterEllipsis"/>

                                <TextBlock Text="{Binding PValueText}"
                                           FontSize="12"
                                           TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>

                <!-- ================= PLOT (FIXED, VIEWABLE) ================= -->
                <Border BorderBrush="{StaticResource PrimaryBrush}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Background="White"
                        Margin="0,0,0,10">

                    <oxy:PlotView Height="420"
                                  Margin="10"
                                  Model="{Binding ECDFPlotModel}"/>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>