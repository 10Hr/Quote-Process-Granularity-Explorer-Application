# Quote Process Granularity Explorer

The Quote Process Granularity Explorer is a specialized analytical tool designed for quantitative researchers and market microstructure analysts working with high-frequency options quote data. Its primary objective is to provide insights into how quote processes vary across different time scales, evaluate the granularity of price discovery mechanisms, and examine temporal characteristics of quote arrival processes in options markets.

This WPF desktop application allows users to load `.arrow` files containing high-frequency option quotes, provides statistical summaries, and performs a **Kolmogorov–Smirnov (KS) test** on the log returns of selected option contracts against a fitted Student’s t-distribution. Users can dynamically configure analyses and export results.

---

## ✨ Observability Rework

This project has recently undergone an **observability rework** using the [Metalama](https://www.postsharp.net/metalama) framework to significantly reduce boilerplate code and improve maintainability.

-   **`[Observable]` ViewModels**: State tracking is now cleanly handled without manual `INotifyPropertyChanged` implementation.
-   **`[Command]` Methods**: ICommand properties and logic bound to UI elements (like `LoadFeatherData`, `ExportMarkdown`, `AddKsTest`) are automatically generated by Metalama, eliminating the noisy `ICommand` boilerplate in the ViewModel and code-behind.
-   **Cleaner XAML bindings**: The XAML views are now purely bound to concise, readable model properties and commands without convoluted event handler logic.

---

## 📁 Features

- **Load Arrow Data**: High-frequency options quote data loading.
- **Summary Statistics**: Displays total quote count, date range, unique contracts, and exchange counts.
- **Contract Filtering**: Select specific contracts by symbol to run tests.
- **Log Return Analysis & KS Test**: Calculates log returns and uses the Regula Falsi method to fit a Student’s t-distribution to the data. 
- **Dynamic Time Windows**: Adjust the time window of the data and automatically recalculate statistics and distributions in real-time.
- **Visualizations**: Displays an Empirical Cumulative Distribution Function (ECDF) plot alongside the fitted t-distribution CDF using OxyPlot.
- **Export Capabilities**:
  - Export KS Test results and an index/price/log-return table in **Markdown (.md)** format.
  - Export ECDF visualizations as **PNG** images.

---

## Architecture & Components

### ViewModels (MVVM powered by Metalama)

- **`MainViewModel.cs`**: Handles the global state of the application. Orchestrates file loading, parsing distinct contract symbols, populating data grids, and triggering the KS Test analysis panels.
- **`RightPanelViewModel.cs`**: Handles the scoped state for a specific analysis. It performs log return calculations, fits the Student's t-distribution using the method of moments and Regula Falsi for root finding, computing the KS test statistic, and plotting the ECDF.

### Views
- **`MainWindow.xaml`**: Hosts the core application layout including the top search bar, left status panel, and houses the dynamic `RightPanel`.
- **`RightPanel.xaml`**: A user control representing a localized analysis session containing sample statistics, distribution parameters, KS test presentation, and the graphic plot. 

### Services
- **`StudentTDistributionZeroMean.cs`**: Custom implementation estimating degrees of freedom (ν) by matching sample kurtosis to theoretical kurtosis via the Regula Falsi root-finding algorithm.
- **`ParseOptionsSymbol.cs`**: Parses complex standard option symbols into accessible underlying, expiration, strike, and type details.
- **`ArrowDataLoader.cs`**: Reads the `.arrow` IPC formatting efficiently.

---

## 📦 Dependencies

- [.NET 8 WPF](https://dotnet.microsoft.com/)
- [Metalama](https://www.postsharp.net/metalama) (for MVVM Observability and Commands)
- [Apache.Arrow](https://arrow.apache.org/) (Data loading)
- [MathNet.Numerics](https://numerics.mathdotnet.com/) (Statistics)
- [Accord.Net](http://accord-framework.net/) (Statistical Testing - Kolmogorov-Smirnov)
- [OxyPlot.Wpf](https://oxyplot.readthedocs.io/en/latest/) (Charting & Visualizations)

---

## 🚀 Usage

1. Download zip file
2. Extract to folder
3. Open **ZSExplorer.exe**
3. Click **"Load Feather Data"** to select a `.arrow` option quote dataset.
4. Using the top **Add Contract** dropdown, search and select an options contract by symbol and strike. 
5. Click **"Add KS Test"** to generate an analysis panel.
6. Use the **Time Window Slider** in the panel to adjust the timeframe of the dataset. Computations will recalibrate instantly.
7. View ECDF plot + summary, and optionally **export results** as image or markdown. 

---

## 🧪 Statistical Methods

### The Kolmogorov–Smirnov (KS) Test
The application uses the KS Test to determine if the empirical distribution of the contract's log returns fits a theoretical zero-mean Student’s t-distribution. It looks at the maximum distance between the Empirical Cumulative Distribution Function (ECDF) and the fitted CDF.

### Regula Falsi Root Finding
To fit the Student's t-distribution, the degrees of freedom (ν) must be found where theoretical kurtosis matches sample kurtosis. Because this cannot be perfectly isolated algebraically for all intervals, the **Regula Falsi** (false position) numerical method is implemented to approximate the root (ν) iteratively.